import { UserListFilterLabelPipe } from './user-list-filter-label.pipe';

describe('UserListFilterLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new UserListFilterLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
